---
layout: layouts/game.njk
---
{% from "partials/prev-next.njk" import prevNext %}

{#
  No inline table of contents any more: the section list lives in the sidebar
  at wide viewports and in the sticky bar below that, so it is available the
  whole way down the page rather than only at the top.
#}
<div class="prose">
  {{ content | enhanceHeadings({
    bookmarkable: true,
    pageUrl: page.url,
    anchorAliases: contentType.anchorAliases
  }) | safe }}
</div>

{% if contentType.sections.length %}
  {{ prevNext(contentType, "inline") }}
{% endif %}
