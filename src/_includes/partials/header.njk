{#
  The header stays deliberately thin — at a table, chrome above the fold is
  chrome between you and the rule you are looking for. It carries identity on
  the left and four controls on the right, and nothing else. The section list
  lives in the sidebar (wide) or the sticky bar (narrow), not up here.
#}
<header class="site-header">
  <div class="site-header__inner">
    {#
      The link is labelled rather than relying on its text, which is hidden at
      narrow widths and whenever a game is open below the sidebar breakpoint.
      Without this the home link has no accessible name exactly where it is the
      only way back.
    #}
    <a class="site-logo" href="/" aria-label="{{ site.title }} — home">
      <span class="site-logo__mark" aria-hidden="true">
        <svg class="site-logo__svg" viewBox="3.5 3.5 25 25" focusable="false">
          <rect class="site-logo__page" x="7" y="6" width="14" height="20" rx="1.5"/>
          <rect class="site-logo__spine" x="20" y="6" width="5" height="20" rx="1.5"/>
          <path class="site-logo__rules" d="M10.5 11h7M10.5 15h7M10.5 19h4"/>
        </svg>
      </span>
      <span class="site-logo__text">{{ site.title }}</span>
    </a>

    {% if game %}
      <span class="site-header__divider" aria-hidden="true"></span>
      <a class="site-header__game" href="{{ game.url }}">{{ game.title }}</a>
    {% endif %}

    <div class="site-header__actions">
      {#
        The site's own pages, shown where the menu that carries them is not.

        Below the sidebar breakpoint these live in the menu; at and above it
        the menu is hidden outright, and until now so were these — Home was
        implied by the wordmark and About was reachable only from the footer,
        which on a rulebook is a long way down. Same two labels as the menu, so
        the two navigations name the site the same way.
      #}
      <nav class="site-nav" aria-label="Site">
        <a class="site-nav__link{% if page.url == '/' %} is-current{% endif %}"
           href="/"{% if page.url == '/' %} aria-current="page"{% endif %}>Home</a>
        <a class="site-nav__link{% if page.url == '/about/' %} is-current{% endif %}"
           href="/about/"{% if page.url == '/about/' %} aria-current="page"{% endif %}>About</a>
      </nav>

      <button
        type="button"
        class="search-trigger"
        data-palette-toggle
        aria-haspopup="dialog"
        aria-expanded="false"
        aria-label="{% if game %}Search {{ game.title }}{% else %}Search all games{% endif %}"
        hidden>
        <svg viewBox="0 0 20 20" width="16" height="16" aria-hidden="true" focusable="false">
          <circle cx="9" cy="9" r="5.5" fill="none" stroke="currentColor" stroke-width="1.8"/>
          <path d="M13.2 13.2 17 17" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
        </svg>
        {#
          Just "Search" — the game's name is already in the header, and
          repeating it here is what crowded a long title out. The full phrase
          stays as the control's accessible name.
        #}
        <span class="search-trigger__label">Search</span>
        <kbd class="search-trigger__key" aria-hidden="true" data-shortcut-key>Ctrl K</kbd>
      </button>

      <button
        type="button"
        class="icon-button"
        data-bookmarks-toggle
        aria-haspopup="dialog"
        aria-expanded="false"
        aria-label="Bookmarks"
        hidden>
        <svg viewBox="0 0 16 20" width="16" height="18" aria-hidden="true" focusable="false">
          <path d="M2.5 1.5h11a1 1 0 0 1 1 1v15.2a.6.6 0 0 1-.94.49L8 14.2l-5.56 4a.6.6 0 0 1-.94-.5V2.5a1 1 0 0 1 1-1Z"
                fill="none" stroke="currentColor" stroke-width="1.6"/>
        </svg>
        <span class="icon-button__badge" data-bookmarks-count hidden></span>
      </button>

      <button
        type="button"
        class="icon-button"
        data-preferences-toggle
        aria-haspopup="dialog"
        aria-expanded="false"
        aria-label="Reading preferences"
        hidden>
        <svg viewBox="0 0 20 20" width="18" height="18" aria-hidden="true" focusable="false">
          <path d="M3 6h9M15 6h2M3 14h2M8 14h9" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/>
          <circle cx="13.5" cy="6" r="2.1" fill="none" stroke="currentColor" stroke-width="1.7"/>
          <circle cx="6.5" cy="14" r="2.1" fill="none" stroke="currentColor" stroke-width="1.7"/>
        </svg>
      </button>

      <button
        type="button"
        class="menu-toggle"
        data-menu-toggle
        hidden
        aria-expanded="false"
        aria-controls="site-menu">
        <span class="menu-toggle__bars" aria-hidden="true"><span></span><span></span><span></span></span>
        <span class="visually-hidden">Menu</span>
      </button>
    </div>
  </div>

  {# The narrow-viewport menu: everything the sidebar carries on a wide one. #}
  <div class="site-menu" id="site-menu" data-menu hidden>
    <div class="site-menu__inner">
      <nav class="site-menu__section" aria-label="Site">
        <p class="site-menu__heading">Site</p>
        <ul class="nav-list">
          {#
            "Home", not "All Games": the page carries the reader's favourites
            and what they were last reading as well as the shelf, and naming it
            after one of its three sections undersells the other two. The shelf
            itself is still headed All Games on the page.
          #}
          <li><a class="nav-link{% if page.url == '/' %} is-current{% endif %}" href="/">Home</a></li>
          <li><a class="nav-link{% if page.url == '/about/' %} is-current{% endif %}" href="/about/">About</a></li>
        </ul>
      </nav>

      {% if game %}
        {% from "partials/nav-macros.njk" import gameSections %}
        <nav class="site-menu__section" aria-label="{{ game.title }}">
          <p class="site-menu__heading">{{ game.title }}</p>
          {{ gameSections(game, page.url) }}
        </nav>
      {% endif %}
    </div>
  </div>
</header>
